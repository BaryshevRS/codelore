# Единый формат ошибок

## Состав

- [errors](../../src/errors.codelore.md) — Единый формат ошибок с машинно-читаемым кодом для всех компонентов Codelore.

**Используется в:** [Точка входа CLI](cli-entrypoint.codelore.md#точка-входа-cli) · [Индексация кода и документации](code-doc-indexing.codelore.md#индексация-кода-и-документации) · [Фасад сервиса Codelore](codelore-service-facade.codelore.md#фасад-сервиса-codelore) · [Перевод документации](doc-translation.codelore.md#перевод-документации) · [Валидация и факт-чекинг сгенерированной документации](llm-doc-validation.codelore.md#валидация-и-факт-чекинг-сгенерированной-документации) · [Пайплайн генерации документации файлов](llm-generation-pipeline.codelore.md#пайплайн-генерации-документации-файлов) · [Разбор JSON-ответов LLM с повтором запроса](llm-json-response-parsing.codelore.md#разбор-json-ответов-llm-с-повтором-запроса) · [Фабрика LLM-провайдера](llm-provider-factory.codelore.md#фабрика-llm-провайдера) · [MCP-сервер Codelore](mcp-server.codelore.md#mcp-сервер-codelore) · [Применение перезаписей секций](section-rewrite-application.codelore.md#применение-перезаписей-секций) · [Общие вспомогательные функции сервиса](service-helpers.codelore.md#общие-вспомогательные-функции-сервиса)

## Зачем это нужно

Предоставляет единый формат ошибок с машинно-читаемым кодом, на который опираются остальные подсистемы при взаимодействии с API-клиентами и UI. Класс CodeloreError фиксирует поля code (тип CodeloreErrorCode) и details, а функция toCodeloreErrorPayload превращает любое исключение в сериализуемый объект CodeloreErrorPayload — это гарантирует, что наружу уходит предсказуемая структура.

## Что делает

Определяет тип ошибки CodeloreError с публичными полями code (из перечисления CodeloreErrorCode) и details. Предоставляет утилиту toCodeloreErrorPayload, которая не модифицирует исходную ошибку и не выполняет логирование, а только возвращает готовый payload. Позволяет другому коду выбрасывать ошибки с осмысленным кодом, не думая о формате сериализации.

## От чего зависит

Не зависит от внешних библиотек — использует только стандартный класс Error. Осознанно не берёт на себя сериализацию, логирование и форматирование сообщений для конечного пользователя; эти задачи возложены на потребителей payload'а.

## Кто и как использует

Любое исключение передаётся в toCodeloreErrorPayload. Если ошибка — экземпляр CodeloreError, извлекаются поля code и details. Иначе функция формирует payload с code = INTERNAL_ERROR и сообщением из error.message. Возвращаемый объект CodeloreErrorPayload готов к JSON-сериализации.

## Чего не делает

Не управляет HTTP-статусами, локализацией сообщений и фильтрацией чувствительных деталей — это зона ответственности вызывающего кода. Ошибки, не являющиеся CodeloreError, всегда получают код INTERNAL_ERROR; чтобы получить другой код, необходимо выбросить экземпляр CodeloreError с нужным значением.
