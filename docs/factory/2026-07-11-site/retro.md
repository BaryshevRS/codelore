What slowed shipping? The saved state schema was version 4 although the older project guidance described version 2.
What caught a real bug? The first live scaffold synchronized zero docs because of that version filter.
Which artifact was never used? The browser screenshot checkpoint could not run because the in-app browser transport failed.
What gets deleted before the next feature? The temporary TDD state file.
